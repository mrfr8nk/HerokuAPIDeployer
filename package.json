{
  "name": "heroku-deployer-on-render",
  "version": "1.0.0",
  "description": "Heroku deployment interface hosted on Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.3.4",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  },
  "engines": {
    "node": "18.x"
  }
}
